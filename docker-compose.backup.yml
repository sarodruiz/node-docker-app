#version: "3"
services:
  node-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
      - /app/node_modules
    #environmentL
    #  - PORT=3000
    env_file:
      - ./.env
